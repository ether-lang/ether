# Test class with default field values
class Person {
    let name
    let age
    let city
    
    def new(n) {
        self.name = n
        self.age = 0
        self.city = "Unknown"
    }
    
    def set_age(a) {
        self.age = a
    }
    
    def set_city(c) {
        self.city = c
    }
    
    def display() {
        print(self.name)
        print(self.age)
        print(self.city)
    }
}

let person = new Person("Alice")
person.display()

person.set_age(25)
person.set_city("Lagos")
person.display()