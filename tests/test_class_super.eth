# Test super method calls
class Base {
    let value
    
    def new(v) {
        self.value = v
    }
    
    def process() {
        print("Base processing:")
        print(self.value)
        return self.value
    }
}

class Derived extends Base {
    def new(v) {
        super(v * 2)
    }
    
    def process() {
        print("Derived processing first")
        let result = super.process()
        print("Derived processing after")
        return result + 10
    }
}

let obj = new Derived(5)
let final_result = obj.process()
print("Final result:")
print(final_result)


# Expected output:
# Derived processing first
# Base processing:
# 10
# Derived processing after
# Final result:
# 20
