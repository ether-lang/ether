let database = {
    "users": [
        {"name": "Alice", "age": 30},
        {"name": "Bob", "age": 25}
    ],
    "active": true
}

print(database["active"])

for user in database["users"] {
    print(user)
}
