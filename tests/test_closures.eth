# Test variable capture in nested functions

def outer(x) {
    def inner(y) {
        return x + y
    }
    return inner(10)
}

let result = outer(5)
print("Closure result:")
print(result)

# Expected output:
# Closure result:
# 15