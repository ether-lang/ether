# Test variable capture in nested functions

fn outer(x) {
    fn inner(y) {
        return x + y
    }
    return inner(10)
}

let result = outer(5)
print("Closure result:")
print(result)

# Expected output:
# Closure result:
# 15